# Optional: Ollama HTTP API base URL (no trailing slash).
# Default if unset: http://127.0.0.1:11434
# Use when Ollama runs on another host or you need to force IPv4 vs localhost.
OLLAMA_BASE_URL=http://127.0.0.1:11434
